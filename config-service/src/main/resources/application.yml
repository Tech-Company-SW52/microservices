server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/Tech-Company-SW52/microservices.git"
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
          default-label: "search-service"
  security:
    user:
      name: root
      password: s3cr3t